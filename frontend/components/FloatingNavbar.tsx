'use client'

import { SiteNav } from './SiteChrome'

/** Kept as the application navigation entry point for existing feature pages. */
export default function FloatingNavbar(_props: Record<string, unknown> = {}) {
  return <SiteNav />
}
